# 基金看板需求文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 基金看板 |
| 版本 | V1.0 |
| 交付形态 | Windows 本地桌面 Web 应用 |
| 访问地址 | `http://127.0.0.1:51888` |
| 数据存储 | 当前电脑浏览器本地存储，不在服务端落库 |
| 目标用户 | 管理个人公募基金持仓、查看实时收益及主要市场指数的用户 |

## 2. 项目背景

基于 `Source` 文件夹中的两个参考项目，重构并定制一个名为“基金看板”的桌面 Web 应用：

- `Source/wzk-fund-main`：复用其基金行情代理、基金识别、份额与净值计算、趋势图、指数行情、本地配置和定时刷新思路。
- `Source/fund-drawdown-recovery-main`：复用其“成本—市值—回本距离”计算逻辑。
- `UI规范/Claude Copy.zip`：作为前端视觉与组件规范的唯一设计依据。

本项目需要将源项目分散的能力收敛为“总览、持仓基金、自选基金、A股大盘、指数看板”五个核心模块，并针对长期持仓收益和回本距离补齐成本数据。

## 3. 关键产品决策

### 3.1 端口定义

`51888` 是唯一面向用户的访问端口。本地后端同时提供：

- 已构建的 Web 静态页面；
- `/api` 行情代理接口；
- `/api/health` 健康检查接口。

不要求用户分别启动前端和后端，也不应要求用户访问其他端口。

### 3.2 本地存储定义

持仓、自选基金、成本、板块及应用设置保存在浏览器 `localStorage`。后端只代理第三方公开行情，不保存用户组合，不建设账号系统和远程数据库。

清理浏览器站点数据、更换浏览器或更换电脑会导致本地数据不可用，因此必须提供 JSON 文件导入和导出。

### 3.3 持仓成本定义

“持仓成本”统一指当前剩余持仓对应的总成本金额，不是单位成本。用户增减仓后可通过编辑持仓手动修正总成本；V1 不建设交易流水和自动成本核算系统。

### 3.4 产品范围

V1 只交付桌面 Web 端，不开发 H5 和微信小程序；包含自选基金、A 股涨跌家数及概念板块涨跌榜，不包含黄金模块。

## 4. 产品目标

1. 用户双击 BAT 后即可在默认浏览器打开基金看板。
2. 用户可维护基金持仓，并在本地长期保留数据。
3. 用户可同时查看累计持仓表现、当日表现和距离回本所需涨幅。
4. 用户可查看 A 股、港股、美股、日韩主要指数及历史走势。
5. 用户可查看 A 股涨跌家数及概念板块涨跌前十，并维护固定顺序的自选基金。
6. 用户可通过 JSON 文件完成备份、迁移和恢复。
7. 桌面页面严格遵循指定 UI 规范，投资数据采用红涨绿跌。

## 5. 非目标

- 不开发 H5、移动端专用页面或微信小程序。
- 不提供账号、登录、云同步、多用户或远程数据库。
- 不提供基金申购、赎回或其他交易能力。
- 不提供交易流水、分红、手续费、税费和先进先出成本核算。
- 不提供投资建议、收益预测或自动调仓建议。
- 不保留黄金模块。
- 不承诺在完全离线状态下获得实时行情；本地存储不等于行情离线可用。

## 6. 信息架构与页面布局

应用采用单页桌面 Web 布局。

### 6.1 顶部横向导航

- 展示“基金看板”品牌标识及名称。
- 导航项按顺序为：总览、持仓基金、自选基金、A股大盘、指数看板。
- 点击导航项滚动或定位到对应模块。
- 当前模块需有清晰但克制的激活状态。

### 6.2 顶部工具栏

从左到右至少包含：

- 页面标题；
- 最近更新时间；
- 刷新按钮；
- 导入数据按钮；
- 导出数据按钮。

页面主内容按“总览 → 持仓基金 → 自选基金 → A股大盘 → 指数看板”的顺序纵向排列。顶部导航不得占用内容区横向空间，持仓基金表格在桌面验收宽度内不显示横向滚动条。

## 7. 功能需求

### 7.1 总览

总览固定展示以下 7 项指标，字段名称及顺序不得擅自更改：

| 顺序 | 指标 | 展示要求 |
|---|---|---|
| 1 | 持仓总市值 | 按最新已确认正式净值计算的全部基金市值合计，保留 2 位小数 |
| 2 | 持仓总成本 | 当前全部基金总成本合计，保留 2 位小数 |
| 3 | 浮动盈亏 | 确认市值减总成本，不计入当日预估涨跌，显示正负号，保留 2 位小数 |
| 4 | 持仓总收益率 | 按确认市值计算的浮动盈亏除以总成本，保留 2 位小数 |
| 5 | 当日收益 | 各基金当天确认或盘中实时收益之和，保留 2 位小数；盘前及非交易日显示 `-` |
| 6 | 当日总收益率 | 当日收益除以前一确认点总市值，保留 2 位小数；盘前及非交易日显示 `-` |
| 7 | 距离回本还需要上涨 | 亏损时显示所需涨幅；盈利或持平时显示“已回本” |

交互和状态要求：

- 正收益和上涨使用红色，负收益和下跌使用绿色，零值使用中性色。
- 金额统一显示人民币符号 `¥`，使用等宽数字并右对齐或基线对齐。
- 数据刷新时保留旧值并显示轻量刷新状态，不允许卡片整体闪烁。
- 任一持仓行情缺失导致汇总不完整时，相关卡片需显示“部分数据缺失”，不得把未知值按 0 计算。
- 无持仓时所有金额显示 `¥0.00`、收益率显示 `0.00%`，“距离回本还需要上涨”显示 `--`。
- 汇总卡片采用紧凑高度，不以大面积留白强调指标。

### 7.2 持仓基金

#### 7.2.1 表格字段

桌面表格固定按以下顺序展示：

| 顺序 | 字段 | 定义 |
|---|---|---|
| 1 | 名称 | 基金名称为主信息，基金代码为次信息 |
| 2 | 持仓成本 | 该基金当前剩余持仓的总成本 |
| 3 | 当前市值 | 份额乘最新已确认正式净值，不计入当日预估涨跌 |
| 4 | 持仓收益率 | `(确认市值 - 持仓成本) / 持仓成本` |
| 5 | 资产占比 | `当前市值 / 持仓总市值` |
| 6 | 板块 | 展示一个或多个简洁标签，无数据时显示 `--` |
| 7 | 实时收益 | 盘中按当日预估净值计算，当天正式净值确认后按正式净值计算；盘前及非交易日显示 `-` |
| 8 | 当日预估涨跌 | 只展示当日预估涨跌数值，不展示迷你走势图；A 股盘前及非交易日显示 `-` |
| 9 | 预估回本还需上涨 | 按当日预估净值计算，黑色文字；达到成本时显示“已回本” |
| 10 | 操作 | 编辑、删除 |

默认按当前市值从高到低排序。表头在长列表滚动时保持可见。

#### 7.2.2 添加持仓

点击“添加持仓”后打开弹窗，字段包括：

- 基金代码：必填，6 位数字；失去焦点或点击识别后拉取名称、净值和板块。
- 持仓成本：必填，大于等于 0，表示当前剩余持仓总成本。
- 当前持仓金额：必填，大于 0，用于按确认净值反推份额。
- 金额口径：必填，可选“昨日结算”或“今日结算”。只有已取得今日确认净值时才允许选择“今日结算”。

保存规则：

- 必须成功取得所选口径对应的确认净值后才能保存。
- 份额按 `当前持仓金额 / 对应确认净值` 计算并保留 4 位小数。
- 同一基金代码不可重复添加。
- 保存成功后关闭弹窗、更新本地存储并刷新行情。
- 保存失败时保留已填写内容，并在对应字段附近说明原因。

#### 7.2.3 编辑持仓

- 可修改持仓成本、当前持仓金额和金额口径。
- 基金代码不可修改；如代码错误，应删除后重新添加。
- 编辑时以当前已计算市值作为“当前持仓金额”的默认值。
- 保存后重新计算份额和全部汇总指标。

#### 7.2.4 删除持仓

- 删除前显示基金名称和代码并要求二次确认。
- 确认后只删除当前基金，不影响其他本地数据。
- 删除完成后立即重算总览和资产占比。

#### 7.2.5 走势详情

- 点击基金名称后打开基金走势弹窗，基金名称需呈现可点击状态。
- 支持近 3 月、近 1 年、近 3 年、成立以来。
- 基金成立时间不足对应周期时隐藏不可用周期。
- 图中显示区间涨跌、高低点、日期和净值；涨跌颜色遵循红涨绿跌。

### 7.3 指数看板

指数必须按以下顺序分组展示：

| 分组 | 指数 |
|---|---|
| A 股 | 上证指数、深证成指、创业板指、北证 50、科创 50、上证 50、沪深 300、中证 500 |
| 港股 | 恒生指数、恒生科技指数 |
| 美股 | 纳斯达克 100、标普 500 |
| 日韩 | 日经 225、首尔综合指数（KOSPI） |

每张指数卡片至少展示：

- 指数名称；
- 最新涨跌幅；
- 数据加载、无数据和错误状态。

交互要求：

- 每个分组有明确标题和分组间距，不把全部指数挤在同一无分组网格中。
- 点击指数卡片打开历史走势弹窗。
- 历史周期支持近 1 月、近 3 月、近 6 月、近 1 年、近 3 年。
- 弹窗展示区间涨跌、高低点、日期和收盘点位。
- 海外市场休市或第三方接口未更新时，保留最近有效值并标注数据日期；不得显示为实时数据。

### 7.4 A股大盘

- 展示上涨、下跌和平盘家数及各自占全部统计股票的比例。
- 以红色表示上涨、绿色表示下跌，并用横向比例条直观展示市场宽度。
- 分栏展示概念板块涨幅前十与跌幅前十，按涨跌幅排序。
- 展示行情源返回的数据时间；接口不可用时显示缺失状态，不以 0 代替未知数据。

### 7.5 自选基金

- 自选基金与持仓相互独立，添加时只要求输入 6 位基金代码。
- 成功识别基金名称和关联板块后写入本地存储。
- 严格按照首次添加顺序固定排列，不按涨跌幅或名称自动重排。
- 删除后再次添加的基金排列到列表末尾。
- 展示基金名称、代码、关联板块、当日预估涨跌及删除操作；当日预估涨跌在 A 股盘前及非交易日显示 `-`，当天无估值点时不展示走势。
- 自选基金随完整 JSON 备份一起导入和导出。

### 7.6 数据刷新

- 首次打开页面后立即请求基金和指数行情。
- 默认每 30 秒静默刷新一次。
- 用户可点击顶部“刷新”立即刷新。
- 显示最近一次成功刷新的北京时间，格式为 `HH:mm:ss`。
- 各模块独立容错；某个接口失败时，其他成功模块继续展示。
- 全部接口失败时，提示“行情加载失败，请确认网络连接或稍后重试”，本地持仓数据不得丢失。
- 页面不可见时可暂停轮询，重新可见后立即刷新一次。

### 7.7 本地数据导出

- 点击“导出数据”后下载完整 JSON 备份文件。
- 文件名格式：`fund-dashboard-backup-YYYYMMDD-HHmmss.json`。
- 导出内容至少包含版本号、导出时间、基金持仓、自选基金和设置。
- 导出不得包含令牌、Cookie、浏览记录或其他与本应用无关的数据。

### 7.8 本地数据导入

- 点击“导入数据”后选择 `.json` 文件。
- 导入前校验 JSON 语法、结构版本、基金代码、成本和份额等字段。
- 校验通过后展示摘要，包括持仓数量、自选数量、导出时间和版本。
- 用户确认后采用整包覆盖方式导入，并明确提示将覆盖当前数据。
- 文件无效或版本不支持时不得修改当前数据，并显示具体错误。
- 导入成功后立即保存到本地、刷新行情并更新页面。

## 8. 数据模型

本地存储键使用 `fund-dashboard-config-v1`，结构如下：

```json
{
  "schemaVersion": 1,
  "settings": {},
  "funds": {
    "000001": {
      "code": "000001",
      "name": "基金名称",
      "shares": 1000.0000,
      "totalCost": 10000.00,
      "sectors": ["板块一"],
      "createdAt": "ISO 8601 时间",
      "updatedAt": "ISO 8601 时间"
    }
  },
  "watchlist": [
    {
      "code": "000002",
      "name": "自选基金名称",
      "sectors": ["板块一"],
      "createdAt": "ISO 8601 时间",
      "updatedAt": "ISO 8601 时间"
    }
  ]
}
```

约束：

- `code`：6 位数字字符串，作为持仓唯一键。
- `shares`：大于 0，最多保留 4 位小数。
- `totalCost`：大于等于 0，最多保留 2 位小数。
- `name`、`sectors`：从行情接口识别后保存，允许后续刷新补全。
- `watchlist`：有序数组，数组顺序即用户添加顺序，基金代码不可重复。
- 实时净值、估值、涨跌幅、走势图和动态市值不属于用户配置，不写入备份文件。
- 所有读取均需经过结构归一化，禁止直接信任导入内容。

## 9. 计算口径

金额计算必须使用高精度十进制运算；禁止用格式化后的两位涨跌幅反推金额。

### 9.1 确认净值与预估净值

1. 确认净值取基金公司已正式披露的最新单位净值。
2. 持仓市值、浮动盈亏、持仓收益率、资产占比及组合回本距离只使用确认净值。
3. 盘中存在有效预估净值时，当日收益和预估回本距离使用预估净值。
4. 无预估净值时，当日动态指标回落到最新可用确认净值；无确认净值时相关持仓指标显示 `--`。

### 9.2 单只基金

```text
当前市值   = 持有份额 × 最新确认净值
浮动盈亏   = 当前市值 - 持仓成本
持仓收益率 = 浮动盈亏 ÷ 持仓成本 × 100%
实时收益   = 持有份额 × (当日预估或确认净值 - 前一确认净值)
资产占比   = 当前市值 ÷ 持仓总市值 × 100%
预估市值   = 持有份额 × 当日预估净值（无预估时取最新确认净值）
预估回本还需上涨 = (持仓成本 - 预估市值) ÷ 预估市值 × 100%
```

当持仓成本为 0 时，持仓收益率显示 `--`。
当预估市值大于等于持仓成本时，“预估回本还需上涨”显示“已回本”。

### 9.3 组合汇总

```text
持仓总市值       = Σ 单只基金当前市值
持仓总成本       = Σ 单只基金持仓成本
浮动盈亏         = 持仓总市值 - 持仓总成本
持仓总收益率     = 浮动盈亏 ÷ 持仓总成本 × 100%
当日收益         = Σ 单只基金实时收益
前一确认点总市值 = Σ (持有份额 × 前一确认净值)
当日总收益率     = 当日收益 ÷ 前一确认点总市值 × 100%
```

### 9.4 回本距离

```text
当持仓总市值 < 持仓总成本：
距离回本还需要上涨 = (持仓总成本 - 持仓总市值) ÷ 持仓总市值 × 100%

当持仓总市值 >= 持仓总成本：
显示“已回本”
```

持仓总市值为 0 或数据不完整时显示 `--`。

### 9.5 精度和格式

- 份额：保存 4 位小数。
- 金额：计算和汇总后按四舍五入保留 2 位小数。
- 百分比：保留 2 位小数；资产占比可保留 1 位小数。
- 正值显示 `+`，负值显示 `-`，零值不显示正号。
- 建议沿用源项目的 `decimal.js`，避免浮点误差。

## 10. UI 与交互规范

### 10.1 设计来源

实现必须以 `UI规范/Claude Copy.zip` 中以下文件为准：

- `colors_and_type.css`：颜色、字体、圆角、间距和阴影令牌；
- `components/*.json` 与 `preview/*.html`：按钮、卡片、输入、标签和导航状态；
- `ui_kits/website/index.html`：桌面 Web 页面组合参考。

不使用其中的移动 App 组合方案作为本项目交付页面。

### 10.2 视觉原则

- 整体气质：温暖、克制、编辑感、可信赖，避免霓虹、玻璃拟态和高饱和科技风。
- 页面以纸张感背景、暖灰文字、米色层级和低对比边框为主。
- 主品牌色使用陶土色：亮色模式 `#C96442`；用于主按钮、激活导航和关键交互，不用于表达基金涨跌。
- 投资数据使用红涨绿跌：亮色模式上涨 `#D7263D`，下跌 `#0B6B4F`，持平使用中性灰。
- 使用间距令牌的 4px 基础节奏，常用间距为 12、16、24、32、48、64px。
- 控件圆角使用 8px 或 12px，内容卡片主要使用 16px，重要大面板可使用 20px 或 24px。
- 阴影只用于轻量分层，不使用强烈悬浮和大面积发光。
- 优先使用背景色层级分隔内容，避免卡片套卡片和重复边框。

### 10.3 字体

- 展示标题：`Newsreader` 字体栈。
- 控件、按钮和短标签：`Poppins` 字体栈。
- 阅读文本：`Lora` 字体栈。
- 金额、百分比、时间和代码：`Geist Mono` 字体栈，并启用等宽数字。
- 中文缺字时使用对应的系统中文衬线或无衬线字体回退，不能出现乱码或方框字。

### 10.4 组件要求

- 主按钮只用于当前区域最重要的操作；导入、导出和编辑使用次级或幽灵按钮。
- 按钮高度在同一工具栏中保持一致，禁用状态保留可读文字。
- 输入框优先通过表面和边框变化表达状态；错误说明紧邻字段。
- 卡片需要清晰的标题、说明、主体和操作层级，不使用密集的多重边框。
- 板块标签保持紧凑，只承担分类作用，不与涨跌色竞争。
- 表格行悬停只使用轻微背景变化，避免位移导致表格抖动。
- 所有可点击图标需有可见提示或 `aria-label`，键盘焦点状态清晰。

### 10.5 桌面适配范围

- 主要设计宽度：1280px 至 1920px。
- 最低验收宽度：1024px。
- 不制作手机底部导航、移动卡片列表或针对触屏的 H5 布局。
- 低于最低宽度时允许主内容横向滚动，但核心操作不得被永久遮挡。

## 11. 后端与接口要求

后端沿用“无状态行情代理”模式，至少提供：

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/health` | 启动脚本确认服务可用 |
| POST | `/api/funds/resolve` | 识别基金名称、板块和确认净值 |
| POST | `/api/funds/quotes` | 批量获取持仓实时行情 |
| GET | `/api/funds/:code/history` | 获取基金历史净值走势 |
| GET | `/api/indices` | 获取分组后的指数行情 |
| GET | `/api/indices/:code/history` | 获取指数历史走势 |

接口要求：

- `/api/indices` 返回项需包含稳定代码、名称、分组、涨跌幅、数据时间或日期。
- 指数历史行情优先使用东方财富；日经 225 在东方财富不可用时使用日经指数官网日线，首尔综合指数在东方财富不可用时使用 Naver KOSPI 日线。
- 后端不读取或写入浏览器本地持仓，不建立用户数据文件。
- 第三方数据源超时应有明确错误，并允许单模块失败。
- 对第三方返回值做类型和有效范围校验，禁止把异常字符串当作数值返回。
- 所有接口只监听本机回环地址 `127.0.0.1`，默认不对局域网或公网开放。
- 页面底部展示“数据仅供参考，不构成投资建议；请以基金公司、交易所及正规行情商披露为准”。

## 12. 一键启动要求

项目根目录提供可双击运行的 `启动基金看板.bat`。

启动流程：

1. 检查 Node.js 是否可用；不可用时显示中文提示并暂停窗口，不能闪退。
2. 检查必要依赖和前端构建产物；首次运行缺失时自动完成安装和构建。
3. 启动本地服务并绑定 `127.0.0.1:51888`。
4. 轮询 `/api/health`，确认成功后调用默认浏览器打开 `http://127.0.0.1:51888`。
5. 如果服务已经运行，则不重复启动，直接打开页面。
6. 如果端口被其他程序占用，显示“51888 端口已被占用”及处理提示，不切换到随机端口。
7. 启动窗口需保留运行日志；关闭该窗口后服务停止。

验收时，用户不需要手动输入命令，不需要先后启动两个终端。

## 13. 非功能需求

### 13.1 兼容性

- Windows 10、Windows 11。
- 当前稳定版及前一个稳定版的 Chrome、Edge。

### 13.2 性能

- 本地页面框架应在 1 秒内出现，行情区域可先展示骨架屏。
- 正常网络下首屏行情目标在 3 秒内完成；第三方接口慢时不得阻塞本地页面展示。
- 基金和指数请求应批量或并行处理，避免逐条串行造成长时间等待。
- 轮询期间不重复创建定时器，不发生明显内存持续增长。

### 13.3 隐私与安全

- 用户组合只保存在当前浏览器站点数据和用户主动导出的 JSON 文件中。
- 不读取、打印、上传或写入任何密钥、密码、令牌及浏览器 Cookie。
- 导入内容需校验并按普通数据处理，不能执行其中的字符串或脚本。
- 基金名称、板块和接口错误在进入页面前需安全转义，防止脚本注入。

### 13.4 可用性

- 加载、空数据、局部错误、全局错误和无网络状态均有明确反馈。
- 用户操作成功或失败后有即时提示。
- 删除和覆盖导入属于破坏性操作，必须二次确认。
- 页面刷新、浏览器重启和服务重启后，本地持仓仍可恢复。

## 14. 验收标准

### 14.1 启动与部署

- [ ] 双击 `启动基金看板.bat` 后自动启动服务并打开浏览器。
- [ ] 页面地址为 `http://127.0.0.1:51888`，核心功能不依赖其他用户可见端口。
- [ ] 服务重复启动、Node.js 缺失和端口占用均有明确中文提示。

### 14.2 本地数据

- [ ] 添加、编辑或删除持仓后，刷新页面及重启服务数据仍存在。
- [ ] 导出文件包含全部持仓成本和份额。
- [ ] 有效备份可恢复数据；无效备份不会覆盖当前数据。
- [ ] 后端目录中不产生用户持仓数据库或组合文件。

### 14.3 总览与计算

- [ ] 总览完整展示指定的 7 个指标，名称和顺序正确。
- [ ] 使用固定测试数据手算后，市值、成本、浮盈、累计收益率、当日收益、当日收益率和回本距离均一致。
- [ ] 金额计算基于净值差，不使用显示用涨跌幅乘持仓金额。
- [ ] 上涨和正收益为红色，下跌和负收益为绿色。

### 14.4 持仓基金

- [ ] 表格完整展示指定的 10 列，顺序正确。
- [ ] 添加基金可识别名称与板块，并正确反推份额。
- [ ] 编辑成本或金额后，总览及资产占比立即更新。
- [ ] 删除前有确认，删除后其余数据不受影响。
- [ ] 当日预估涨跌只显示数值，A 股盘前及非交易日显示 `-`；点击基金名称可打开历史走势弹窗。
- [ ] 确认净值汇总不受盘中预估涨跌影响，预估回本距离会计入当日预估净值。

### 14.5 指数看板

- [ ] 按 A 股、港股、美股、日韩四组展示。
- [ ] 恒生指数、恒生科技指数、日经 225、首尔综合指数均有行情卡片。
- [ ] 指数卡片可打开 1 月至 3 年的历史走势。
- [ ] 休市或数据延迟时展示数据日期，不误标为实时。

### 14.6 A股大盘与自选基金

- [ ] A股大盘展示涨跌平家数、比例条及概念板块涨跌前十。
- [ ] 自选基金可添加和删除，刷新及重启后仍按添加顺序固定排列。
- [ ] JSON 导入导出同时包含持仓和自选基金。

### 14.7 UI

- [ ] 1024px、1280px、1440px、1920px 宽度下无关键内容遮挡或重叠。
- [ ] 颜色、字体、间距、圆角、阴影、按钮、输入和导航遵循指定 UI 规范。
- [ ] 主品牌陶土色与投资涨跌红绿语义没有混用。
- [ ] 导航为顶部横向布局，持仓基金表格无需横向滚动即可完整展示 10 列。
- [ ] 未实现 H5 或小程序入口、布局和构建产物。

## 15. 交付物

- 可运行的前端与本地后端源代码。
- 前端生产构建产物。
- `启动基金看板.bat`。
- 简明使用说明，包含启动、停止、导入、导出和数据丢失风险提示。
- 计算逻辑与导入校验相关的自动化测试。
